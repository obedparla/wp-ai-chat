[
  {
    "category": "Functional",
    "project": "provider",
    "description": "Provider admin settings page displays the site key and allows configuring the OpenAI API key. Minimal UI — just enough to copy the site key and set the API key without touching the database directly.",
    "steps": [
      "Open provider plugin admin settings page in WordPress",
      "Verify the auto-generated site key is displayed (read-only, copyable)",
      "Verify a 'Copy' button or similar mechanism exists for the site key",
      "Verify an OpenAI API key input field exists (password-masked)",
      "Save a new API key and verify it persists after page reload",
      "Verify the default model setting is displayed and editable",
      "Verify settings are sanitized on save (API key trimmed, model validated)"
    ],
    "passes": false
  }
]
